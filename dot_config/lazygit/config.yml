gui:
  nerdFontsVersion: "3"

git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never

  branchLogCmd: "git log --graph --oneline --decorate --color {{branchName}}"

os:
  editPreset: 'nvim'

keybinding:
  universal:
    nextTab: 'ø'
    prevTab: 'æ'

